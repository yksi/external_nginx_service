server {
    listen 8090;
    listen [::]:8090;

    server_name _;

    location / {
        include /etc/nginx/subconf.d/cors.conf;

        set $upstream   messaging_nginx;
        proxy_pass      http://$upstream;
    }
}