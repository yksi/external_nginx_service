server {
    listen 8007;
    listen [::]:8007;

    server_name _;

    location / {
        set $upstream   eservia-delivery-dashboard;
        proxy_pass      http://$upstream;
    }

    include /etc/nginx/subconf.d/catch.conf;
}